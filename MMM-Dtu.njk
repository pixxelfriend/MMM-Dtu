<div class="MMM-DTU {{ borderClass }}">
    <div class="header">
        <i class="fas fa-sun"></i>
        {{ text.SOLAR_PRODUCTION }}
    </div>
    <div class="body">
        <table>
            <thead>
                <tr>
                    <th></th>
                    {% for inverter in inverters %}
                        <th>{{ inverter.name }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tr>
                <td>{{ text.CURRENT_DC }}</td>
                {% for inverter in inverters %}
                    <td>{{ inverter.values.P_DC }}</td>
                {% endfor %}
            </tr>
            <tr>
                <td>{{ text.FEED_IN }}</td>
                {% for inverter in inverters %}
                    <td>{{ inverter.values.P_AC }}</td>
                {% endfor %}
            </tr>
            <tr>
                <td>{{ text.TODAY }}</td>
                {% for inverter in inverters %}
                    <td>{{ inverter.values.YieldDay }}</td>
                {% endfor %}
            </tr>
            <tr>
                <td>{{ text.TOTAL }}</td>
                {% for inverter in inverters %}
                    <td>{{ inverter.values.YieldTotal }}</td>
                {% endfor %}
            </tr>
            <tr>
                <td>{{ text.UPDATE }}</td>
                {% for inverter in inverters %}
                    <td>{{ inverter.lastUpdate }}</td>
                {% endfor %}
            </tr>
            <tr>
                <td>{{ text.STATUS }}</td>
                {% for inverter in inverters %}
                    <td>{{ text[inverter.status] }}</td>
                {% endfor %}
            </tr>
        </table>
    </div>
    <div class="footer">
        {% if connected %}
            <i class="fas fa-history"></i>
            {{ lastUpdate }}
        {% endif %}
        {% if not connected %}
            <i class="fas fa-history"></i>
            {{ text.CONNECTING }}
        {% endif %}
        {% if error %}
            <br/> {{ text.CONNECTION_ERROR }}
        {% endif %}
    </div>
</div>